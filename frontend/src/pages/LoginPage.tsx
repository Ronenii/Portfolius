export default function LoginPage() {
  return (
    <section className="page-section" aria-labelledby="login-title">
      <p className="eyebrow">Authentication</p>
      <h1 id="login-title">Sign in</h1>
    </section>
  );
}
